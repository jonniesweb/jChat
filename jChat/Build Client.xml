<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="create_run_jar" name="Create Runnable Jar for Project jChat">
	<!--this file was created by Eclipse Runnable JAR Export Wizard-->
	<!--ANT 1.7 is required                                        -->

	<target name="create_run_jar">


		<propertyfile file="build.properties">
			<entry key="build.number" type="int" operation="+" default="0" />
		</propertyfile>
		<property file="build.properties" />
		<echo>Build number is ${type}-${major}.${minor}.${build.number}</echo>

		<jar destfile="${destination}/jChatClient-${type}-${major}.${minor}.${build.number}.jar" filesetmanifest="mergewithoutmain">
			<manifest>
				<attribute name="Main-Class" value="jChat.EnterDialog" />
				<attribute name="Class-Path" value="." />
				
			</manifest>
			<fileset dir="/home/jonnie/git/jChat/jChat/bin" />
			
		</jar>

	</target>
</project>
